#include "RecommendationLogic.h"

std::vector<std::string> FilterAndSortRecommendations(const std::vector<std::pair<std::string, int>>& productScores) {
   //the recommendation logic will be implemented here in the future, but for now we return an empty vector to make the test run and fail.
    // right now the test will fail because the function is not implemented yet, but once we implement the function, the test should pass if the implementation is correct.
    return {};
}