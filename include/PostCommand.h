#ifndef POST_COMMAND_H
#define POST_COMMAND_H

#include <sstream>
#include "DataManager.h"
//executePostCommand decleration.
void executePostCommand(std::istringstream& iss, DataManager& dataManager);

#endif